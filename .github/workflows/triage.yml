name: Triage

permissions:
  contents: read
  pull-requests: write
  issues: write

on:
  issues:
    types:
      - opened
      - reopened

  pull_request_target:

jobs:
  triage:
    runs-on: ubuntu-latest
    name: Label

    steps:
      - name: Apply labels
        uses: actions/labeler@v3
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
